spring:
    application:
        name: url-shortener
    datasource:
        url: jdbc:postgresql://localhost:5432/urlshortener
        username: ${DB_USERNAME}
        password: ${DB_PASSWORD}
    jpa:
        hibernate:
            ddl-auto: validate